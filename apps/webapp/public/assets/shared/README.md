# Shared Audio Assets

Place the following audio files here for game sound effects:

- `tap.mp3` — UI tap / button press
- `success.mp3` — Win / score milestone
- `fail.mp3` — Game over / loss
- `coin.mp3` — Coin / ticket earned

The PreloadScene handles missing audio gracefully (loaderror handler), so the app works without these files.
